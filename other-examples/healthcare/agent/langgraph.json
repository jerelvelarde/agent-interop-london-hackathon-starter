{
  "python_version": "3.12",
  "dockerfile_lines": [],
  "dependencies": ["."],
  "package_manager": "uv",
  "graphs": {
    "healthcare_agent": "./healthcare_agent/graph.py:graph"
  },
  "env": "../../../.env",
  "_note": "Standalone langgraph.json for the healthcare example. The dir name `healthcare_agent/` matches the package name, so this entry uses canonical package-relative imports (`from .tools import ...` inside graph.py) — no sys.path hacks needed. Pattern mirrors other-examples/legal-contract-review/agent/langgraph.json. NOTE: this file is NOT used by the project-root `pnpm dev` flow today; the main `agent/langgraph.json` still owns dev-server graph registration."
}
