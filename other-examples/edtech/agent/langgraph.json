{
  "python_version": "3.12",
  "dockerfile_lines": [],
  "dependencies": ["."],
  "package_manager": "uv",
  "graphs": {
    "edtech_agent": "./edtech_agent/graph.py:graph"
  },
  "env": "../../../.env",
  "_note": "Standalone langgraph.json for the ScholarAI (edtech) example. The dir name `edtech_agent/` matches the package name, so this entry uses canonical package-relative imports (`from .tools import ...` inside graph.py) — no sys.path hacks needed. See audit item #6. This file documents the canonical entry point and is used by the standalone Dockerfile; the project-root `agent/langgraph.json` still owns dev-server graph registration today."
}
